programs:
  - name: slackMessaging
    path: /app/messenger
    user: app-user
    group: app-user
    capabilities:
      - CAP_NET_BIND_SERVICE
    env:
      - RUNTIME_SERVIS_SELF_PORT:
      - RUNTIME_SLACK_API_KEY:
      - RUNTIME_MESSAGE_API_KEY:

