stages: [lint, test, build]
lint:
  image: golangci/golangci-lint:v1.53
  script: golangci-lint run
test:
  image: golang:1.21
  script:
    - go test ./... -coverprofile=coverage.out
build:
  image: golang:1.21
  script:
    - go build -o messenger ./cmd/your-service