/*
Package smtpproxy contains all code for an SMTP proxy, which exposes an SMTP interface
(potentially even without authentication etc) that transparently forwards the request
to a centrally managed notifications API for auditing, logging, security, permission
management, ...
*/
package smtpproxy
